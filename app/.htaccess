<IfModule mod_rewrite.c>
            RewriteEngine On
            RewriteBase /projek_apotek/# Penting: Periksa ini!
            RewriteCond %{REQUEST_FILENAME} !-F
            RewriteCond %{REQUEST_FILENAME} !-d
            RewriteRule ^(.*)$ index.php/$1 [L]
        </IfModule>
